---
id: DESIGN-NN
title: 詳細設計書 — （対象領域）
status: draft   # draft → approved
created: YYYY-MM-DD
requirements: []      # 対応する要件ID
plan: PLAN-NNN        # この設計を参照する計画書（複数可）
---

# DESIGN-NN: タイトル

**任意層の文書。毎回は書かない。** 以下のトリガーに該当するときだけ作成する。

- 要件定義と計画書の間の飛躍が大きく、実装者（AI）が迷う複雑な機能
- 複数の計画書にまたがって参照される共通基盤・アルゴリズム
- 状態遷移・データフローが複雑で、文章より図表で固めるべきもの

## なぜこの詳細設計書を書くか

上記トリガーのどれに該当するか。

## モジュール構成

ファイル / クラス / 関数レベルの構成と責務、依存関係。

## データモデル詳細

内部データの型定義・バリデーションルール。
（外部IFに関わるDBスキーマは `docs/interfaces/DB.md` が正。ここからは参照のみ）

## インターフェース詳細

内部コンポーネント間のIF、画面項目、エラーコード等。
（外部公開APIは `docs/interfaces/API.md` が正。ここからは参照のみ）

## 処理フロー

主要な処理の流れ。分岐・例外処理を含む。状態遷移・データフローは図で描く。

## 設計判断と理由（Why this design）

**この詳細設計に至った理由を必ず書く。** ADRトリガーに該当する判断は
`docs/adr/` に起票してここからID参照する。

- 判断1: （内容）。理由: 。代替案: （採らなかった理由）。関連: ADR-NNNN

## 変更履歴

| 日付 | 変更内容 | 理由・経緯 |
|---|---|---|
| YYYY-MM-DD | 初版作成 | |

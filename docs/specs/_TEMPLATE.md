---
id: SPEC-NN
title: 仕様書・基本設計書 — （対象領域）
status: deprecated   # この層は廃止（2026-07-07、下記参照）
created: YYYY-MM-DD
requirements: [REQ-F-01, REQ-F-02]   # この仕様がカバーする要件ID
---

> **【廃止】** この「仕様書・基本設計書」層は2026-07-07に廃止された。
> 理由: 個人＋AI開発では要件定義書・計画書と内容が重複し、文書メンテが破綻するため。
> 役割は以下に移管された。新規文書はこのテンプレートから作らないこと。
> - 全体設計・設計方針 → 計画書（`docs/plans/`）の「実装アプローチ・アーキテクチャ」
> - API/DBの外部IF仕様 → 常設仕様書（`docs/interfaces/API.md`, `DB.md`）
> - 複雑な機能の詳細 → 詳細設計書（`docs/designs/`、トリガー該当時のみ）

# SPEC-NN: タイトル

## 対応する要件

| 要件ID | この文書での実現方法（節への参照） |
|---|---|
| REQ-F-01 | §機能仕様-1 |

## 全体構成

システム構成・主要コンポーネントとその責務。図が有効なら Mermaid 等で描く。

## 機能仕様

要件を「どういう振る舞いで実現するか」を定義する。入出力・正常系・異常系。

### 機能1: 

## 外部インターフェース概要

画面 / API / CLI / ファイル入出力など、外から見える境界の一覧。

## データ概要

扱う主要データとその関係（詳細は詳細設計書で定義）。

## 設計方針と理由（Why this design）

**この設計に至った理由を必ず書く。** 検討した代替案と採らなかった理由も残す。
ADR相当の大きな判断は `docs/adr/` に起票し、ここからID参照する。

- 方針1: （内容）。理由: 。代替案: （採らなかった理由）。

## 変更履歴

| 日付 | 変更内容 | 理由・経緯 |
|---|---|---|
| YYYY-MM-DD | 初版作成 | |
